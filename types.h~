
#ifndef TYPES_H
#define TYPES_H

#include <pthread.h>
#include <queue>
#include <semaphore.h>

enum RequestType { GENERAL, VIP };

struct SharedData
{
    std::queue<RequestType> requests;
    pthread_mutex_t lock;
    sem_t consumed;
    sem_t unconsumed;
};

#endif
